# fastapi-minimal

A hello-world API with FastAPI + Uvicorn.

## Quickstart

```bash
python -m venv .venv
# Win: .venv\Scripts\Activate  |  Linux: source .venv/bin/activate
pip install -e .
serve
# or: uvicorn app.main:app --reload
```
